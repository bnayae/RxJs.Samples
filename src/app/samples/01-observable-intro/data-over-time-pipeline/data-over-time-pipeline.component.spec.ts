/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DataOverTimePipelineComponent } from './data-over-time-pipeline.component';

describe('Component: DataOverTimePipeline', () => {
  it('should create an instance', () => {
    let component = new DataOverTimePipelineComponent();
    expect(component).toBeTruthy();
  });
});
